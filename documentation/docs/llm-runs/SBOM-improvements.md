Need to improve SBOM so it's more accurate and complete for workflows like policy evaluation and behavior validation, redteam testing. This includes the following improvements:
- **Component Name**: Ensure that the component name is accurate and descriptive, providing clear information about the purpose and functionality of each component. This can help improve the clarity and usefulness of the SBOM for policy evaluation and behavior validation. The LLM can be used to generate accurate and descriptive component names based on the functionality and purpose of each component. E.g. instead of saying "generic" be more specific and unique, such as "User Authentication API" or "Customer Data Storage Service". 
- **Rate Limit Detials**: Add more detailed information about rate limits, such as specific limits for different API endpoints, time windows for rate limits, and any exceptions or special cases. Pay close attention to the rate limits for the LLM calls, as these can significantly impact the performance and reliability of the system. Store these details in a structured format that can be easily accessed and utilized during policy evaluation and behavior validation. Store the relationship between the target component and the rate limit details in a way that allows for easy retrieval and analysis. E.g. for API endpoints, models that are part of an agent, or other components that have specific rate limits.
- **Authentication and Authorization Details**: Include information about the authentication and authorization mechanisms used by each component, such as the types of credentials required, the authentication protocols supported, and any access control policies in place. This information can help identify potential security risks and ensure that appropriate measures are in place to protect sensitive data and resources. Store the relationship between the target component and the Auth details in a way that allows for easy retrieval and analysis. E.g. for API endpoints, models, datastores, or other components that require authentication and authorization.
- **Encryption or Redaction Details**: Include information about any encryption or redaction mechanisms used by each component, such as the types of data that are encrypted or redacted, the encryption algorithms or redaction techniques used, and any key management practices in place. This information can help identify potential security risks and ensure that appropriate measures are in place to protect sensitive data. Store the relationship between the target component and the encryption/redaction details in a way that allows for easy retrieval and analysis. E.g. for API endpoints, models, datastores, or other components that handle sensitive data.
- **Data Handling Practices**: Include information about the data handling practices of each component, such as how data is collected, stored, processed, and shared. This information can help identify potential privacy risks and ensure that appropriate measures are in place to protect user data. Store the relationship between the target component and the data handling practices in a way that allows for easy retrieval and analysis. E.g. for API endpoints, models, datastores, or other components that handle user data.
- **Dependency Information**: Include detailed information about the dependencies of each component, such as the specific libraries, frameworks, or services that are used. This information can help identify potential vulnerabilities or issues. Store the relationship between the target component and the dependency details in a way that allows for easy retrieval and analysis. E.g. for API endpoints, models, datastores, or other components that rely on external dependencies.
- **Instrucmentation Details**: Include information about the instrumentation of each component, such as the types of metrics collected, the logging practices, and any monitoring or alerting mechanisms in place. This information can help identify potential issues and ensure that appropriate measures are in place to maintain the performance and reliability of the system. Store the relationship between the target component and the instrumentation details in a way that allows for easy retrieval and analysis. E.g. centralized logging, metrics collection, and alerting systems.
- **Testing and Validation Details**: Include information about the testing and validation practices, such as the types of tests performed, the testing frameworks used, and any validation criteria or benchmarks. For CI/CD, include details about the integration and deployment pipelines, automated tests, and any quality gates or approval processes in place. Store the relationship between the target component and the testing and validation details in a way that allows for easy retrieval and analysis. E.g. for API endpoints, models, datastores, or other components that undergo testing and validation.
- **LOC**: calculate the lines of code (LOC) for each component and include this information in the SBOM. This can help identify potential complexity and maintenance issues, as well as provide insights into the development effort required for each component. Store the overall LOC for the application.
- **Tokens**: Calculate the input and output tokens for the repo if the LLM option is enabled in nuguard config.
