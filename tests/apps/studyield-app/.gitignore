# Secrets & credentials - NEVER commit these
.env
.env.*
!.env.example
*.key
*.pem

# Cloned Studyield source (no published image exists — we build from source)
repo/

# NuGuard-generated artifacts
*.sbom.json
!studyield.ground-truth.sbom.json
cognitive-policy.md
cognitive-policy.json
reports/*.md
reports/*.json
reports/*.log
!reports/.gitkeep

# Seed data output (real, live account credentials + resource IDs)
seed-data.json
canary.json

# Studyield runtime data
data/
logs/
