"""Phase 5 — adaptive execution (thin layer over v1 executors).

Translates a scheduled objective into an ``ExploitChain`` or
``GuidedConversation`` and runs it through the existing ``AttackExecutor`` /
``GuidedAttackExecutor``, adding adversary-loop and kill-chain composition.

Not implemented yet (scaffold).
"""
