# Update Behavior and Redteam validation runs with new SBOM and few other updates
1. **Behavior Validation Runs**: Update the existing behavior validation runs to incorporate the new SBOM. This will ensure that the tests are aligned with the latest software components and dependencies. For example, use the details of a prompt to build test cases for the associated agent along with all the SBOM details and edges related to the agent. Similarly, use the details of API endpoints including the JSON schema to build test cases.
2. **Redteam Validation Runs**: Similarly, update the redteam validation runs to use the new SBOM, ensuring that the security assessments reflect the current state of the software. For example, use the details of a guardrail to build offensive test cases for the agents that are protected by the guardrail. Use the edge relationships to build test cases for the agents that are protected by the guardrail. Similarly, use the details of API endpoints including the JSON schema to build offensive test cases.
3. **Policy Check**: Use the `nuguard policy check` command with the updated SBOM to validate the behavior against the new software inventory.
4. **Reporting**: Ensure that the instrumentation for both behavior and redteam runs is updated to capture relevant data based on the new SBOM, such as specific vulnerabilities or dependencies that may be present in the software components. This will help in generating accurate and insightful reports for stakeholders.
5. **Instrumentation**: Update llm client instrumentation to capture additional metrics related to input and output tokens. Total tokens (input + output) are calculated and recorded for each run of SBOM generation, Policy check, Behavior and Redteam validation runs.

## Coding Guidelines
1. Follow the existing code structure and conventions used in the current implementation.
2. Ensure that the new SBOM is properly integrated into the behavior and redteam validation runs, and that all relevant details are captured in the test cases.
3. Write clear and concise code, with appropriate comments and documentation to explain the logic and functionality.
4. Implement error handling to manage any potential issues that may arise during the validation runs, such as missing SBOM details or failed test cases.
5. Ensure that the code is modular and maintainable, allowing for easy updates and modifications in the future as needed.
6. Optimize the code for performance, especially when dealing with large SBOMs or complex test cases, to ensure that the validation runs are efficient and do not consume excessive resources.